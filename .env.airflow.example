# Airflow Environment Configuration Template
# Copy this file to .env.airflow and customize the values as needed

# User ID for Airflow processes (default: 50000)
AIRFLOW_UID=50000

# Project directory (default: current directory)
AIRFLOW_PROJ_DIR=.

# Admin user credentials (change these in production!)
_AIRFLOW_WWW_USER_USERNAME=airflow
_AIRFLOW_WWW_USER_PASSWORD=change_me_in_production

# Fernet key for encryption (generate new key with: openssl rand -base64 32)
# Note: This is already set with a default in docker-compose.airflow.yml
# Only override if you need a specific key
#AIRFLOW__CORE__FERNET_KEY=your_generated_fernet_key_here